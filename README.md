# AI Travel Planner (MVP Skeleton)

## 1) 설치
```bash
npm install
cp .env.local.example .env.local
# .env.local에 Google Maps API 키 입력
```

## 2) 실행
```bash
npm run dev
```

## 3) 페이지
- `/`: 검색 폼
- `/itinerary`: 일정 결과 + 지도 뷰
